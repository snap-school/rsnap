.navbar.navbar-default.navbar-fixed-top.navbar-inverse
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to "RSnap", root_path, :class=>"navbar-brand"
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li{:class=>resource_active("missions")}
          = link_to "Missions", missions_path
        -if user_signed_in?
          %li{:class=>resource_active("programs")}
            = link_to "Programmes", programs_path

      .nav.navbar-nav.navbar-right
        =render "layouts/user_menu"
