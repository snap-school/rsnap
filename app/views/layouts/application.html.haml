!!!
%html{lang: "fr"}
  %head
    = render "layouts/meta", title: @title

    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    / HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries
    /[if lt IE 9]<script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    /[if lt IE 9]<script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  %body
    #wrap
      = render "layouts/header"

      / Begin page content
      #body
        .container
          - flash.each do |name, msg|
            - name = :info if name == :notice || name == "notice" || name == :user || name == "user"
            - name = :danger if name == :alert || name == "alert"
            .alert.alert-dismissable{class:  "alert-#{name}"}
              %button.close{type: "button", "data-dismiss"=>:alert, "aria-hidden"=>true}
                &times;
              = msg

          = yield

          = render "layouts/debug"

    #footer
      .container
        %p.text-muted.credit
          RSnap : Simon Hock et anciennement Gaetan Collart & Simon Claessens :
          = link_to "Qui sommes-nous ?", about_home_index_path

