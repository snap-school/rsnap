-if user_signed_in?
  - content = capture do
    %p
      =button "Mes informations", user_path(current_user), type: :primary
    %p
      =button t_attr(:user, :sign_out), destroy_session_path(resource_name), :method => :delete,
              type: :warning, :id=>:sign_out
  -btn_txt = current_user.name
-else
  -content = capture do
    #login-menu
      =render "devise/sessions/login_form"
  -btn_txt = "Se connecter"

%a.btn.navbar-btn.btn-success.popover-default#user-menu{"data-content"=>content ,:title=>btn_txt, "data-toggle"=>:popover, "data-placement"=>:bottom}
  =btn_txt
=#button btn_txt, "#", :class=>"navbar-btn popover-default", type: :success, :id=>"user-menu", "data-content"=>content ,:title=>btn_txt, "data-toggle"=>:popover, "data-placement"=>:bottom
-unless user_signed_in?
  =button "Créer un compte", new_registration_path(resource_name), type: :info, :class=>"navbar-btn"
