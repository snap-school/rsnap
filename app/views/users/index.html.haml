%h1= @title

%table.table.table-striped.table-condensed.table-bordered.table-hover
  %thead
    %tr
      -if current_user.try(:has_role?, :admin)
        %th Rôle
      %th Prénom
      %th Nom
      %th Email
      %th
      %th Programmes
      -if current_user.try(:has_role?, :admin)
        %th
        %th
  %tbody
    = render @users

%p
  -if current_user.try(:has_role?, :admin)
    = button "Créer un utilisateur", new_user_path, type: :success
